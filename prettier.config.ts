import type { Config } from "prettier";

/**
 * @see https://prettier.io/docs/configuration
 */
const config: Config = {
  trailingComma: "es5",
  semi: true,
  singleQuote: false,
};

export default config;
